# GUI for SenseNet Project Installer

This is a GUI interface for SenseNet-Project-Installer. It build in Electron framework.
App settings: PSscripts\Scripts\Settings\GUISettings.json

Install:
npm install

Build:
npm run build

Run:
npm start