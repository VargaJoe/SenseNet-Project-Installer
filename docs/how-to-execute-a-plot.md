# Execute a plot

See [configuration and examples](settings.md) and [the README](../README.md) for the native runtime. Use -Explain to validate the plan and -WhatIf to skip operations.

The historical global-settings guide is preserved in [legacy/how-to-execute-a-plot.md](legacy/how-to-execute-a-plot.md); those commands explicitly require -Legacy.
